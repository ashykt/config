if has('gui_macvim')
	syntax on
	set noimdisableactivate
	set imdisable
	set lines=50
	set columns=100
	colorscheme landscape
	colorscheme molokai
	hi Visual  guifg=#000000 guibg=#FFFFFF gui=none
endif
